package database

import "fmt"

/*
	This package is responsible for all database operations, it should create a DB Struct
	which will hold the database connection and information. It is responsible for:
	- Adding messages to the database
		- given a specific chat room, sender name, time sent, and message
	- reading messages from the database
		- read them from a specific chat room
*/

// main function of database
func main() {
	fmt.Println("database main")
}

// function to load the database into memory

// function to save the database onto disk

// function to add a message to the database

// function to get all the messages in a specific chatroom
