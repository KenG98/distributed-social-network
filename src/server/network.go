package server

import "fmt"

func StartNetwork() {
	fmt.Println("Starting network server")
}

/*
	This is responsible for the network node on a user's computer which supports the network.
	It should be able to start a new network, join an existing network, accept other nodes which want
	to join the network, and send and recieve messages amongst the nodes.
*/

// create and start serving with an http server

// start your own network

// join an existing network by connecting to one node which reveals the rest of the network

// path to reveal the network to another node

// send a message out to the network

// get a message from a node on the network

// set a user identity (save a nickname)
